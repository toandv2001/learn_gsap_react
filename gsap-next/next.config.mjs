/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos','images.unsplash.com','assets.aceternity.com','aceternity.com'],
    },
};

export default nextConfig;
